#include "main.h"
#include <stdio.h>

/**
 * puts2 - prints every other character
 * @str: string
 *
 * Return: nothing
 */

void puts2(char *str);
{
	i = 0;
	while(*(str + i) != *\0*)
	{
		if (i % == 0)
		       puchar(*(str + i));
		i++;
	}
	puchar(10);
}
