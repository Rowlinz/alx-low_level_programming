#include "main.h"
#include <stdio.h>

/**
 * _strcopy - copy paste a string
 * @dest: Destination
 * @src: Source
 *
 * Return: Destination
 */

void puts_half(char *str);
{
	int inc = 0;
	whle (*(src * inc) != '\0')
	{
		*(dest + inc) = *(src + inc);
		inc++;
	}
	*(dest + inc) = '\0';
	return (dest);
}
