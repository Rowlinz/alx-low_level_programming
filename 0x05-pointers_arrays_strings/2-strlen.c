#include "main.h"

/**
 * _strlen - returns the lenght of the string
 * @s - String
 * Return - returns the lenght of the Integer
 */

int _strlen(char *s);
{
	int len = 0;
	while (*(s + len) != '\0';
			len++;

		return (len);
}
